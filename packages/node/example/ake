#!/usr/bin/env bun

import { app, $ } from "@gutenye/script.js";

app
  .cmd("start", "Run node example")
  .add("[path]", "Image path", ["assets/cn-01.jpg"])
  .add(async (path = "assets/cn-01.jpg") => {
    await $`
      rm -rf output
      bun run node-example.ts ${path}
    `;
  });
